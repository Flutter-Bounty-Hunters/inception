name: inception
description: A code editing toolkit for Flutter
version: 0.0.1
homepage: https://github.com/Flutter-Bounty-Hunters/inception

environment:
  sdk: ">=2.17.6 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  analyzer: ^4.6.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
